# Vercel Site

The repo root contains `index.html`, so Vercel can deploy this repository directly without a build step.

Edit `site/config.js` and replace:

```js
appsheetUrl: "https://www.appsheet.com/start/YOUR_APP_ID"
```

with your AppSheet browser link.

In AppSheet, enable **Security > Options > Allow app embedding**.
